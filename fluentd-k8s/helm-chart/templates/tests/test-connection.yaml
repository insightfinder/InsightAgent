apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "if-fluentd.fullname" . }}-test-connection"
  labels:
    {{- include "if-fluentd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "if-fluentd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
